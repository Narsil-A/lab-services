/** @type {import('tailwindcss').Config} */
export const content = [
  'core/templates/core/**/*.html', // All HTML files in the core app's templates
  'dashboard/templates/dashboard/**/*.html', // All HTML files in the dashboard app's templates
  'userprofile/templates/userprofile/**/*.html' // All HTML files in the userprofile app's templates
];

export const theme = {
  extend: {},
};

export const plugins = [];
